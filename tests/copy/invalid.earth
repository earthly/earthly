VERSION --copy-parent-dirs 0.6

FROM alpine:3.13

WORKDIR /test

multi-parent-context:
    COPY ../test1 ../../opt/test2 ./

# this test is failing, COPYING ".." should probably not be allowed
all-parent-dir:
    COPY .. .
