VERSION 0.6

FROM alpine:3.13

WORKDIR /test

all:
    BUILD +multi-parent-context
    BUILD +all-parent-dir

multi-parent-context:
    COPY ../test1 ../../opt/test2 ./

all-parent-dir:
    COPY .. .
