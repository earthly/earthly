version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
      docker_layer_caching: true
    steps:
      - checkout
      - run: ./.circleci/gh-dl-release.sh v0.0.1 earth-linux-amd64
      - run: sudo mv ./earth-linux-amd64 /usr/bin/earth
      - run: sudo chmod +x /usr/bin/earth
      - run: earth --help
